import { Box } from "@mui/material";
import React from "react";
import styles from "./HeroSection.module.scss";

const HeroSection = () => {
  return <Box className={styles.heroContainer}>HeroSection</Box>;
};

export default HeroSection;
